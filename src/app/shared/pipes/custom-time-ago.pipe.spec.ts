import { CustomTimeAgoPipe } from './custom-time-ago.pipe';

describe('CustomTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
